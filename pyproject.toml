[project]
name = "pbl"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.135.2",
    "pycparser>=3.0",
    "pytest>=9.0.2",
    "uvicorn>=0.42.0",
]
[tool.setuptools]
packages = ["compiler"]   
